<phone:PhoneApplicationPage
    x:Class="StopWatch.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:UI="clr-namespace:Microsoft.Advertising.Mobile.UI;assembly=Microsoft.Advertising.Mobile.UI"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="False">

    <Viewbox>
        <!--LayoutRoot is the root grid where all page content is placed-->
        <Grid x:Name="LayoutRoot" Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

            <UI:AdControl 
                 x:Name="AdControl"
                 ApplicationId="5c0b9569-8b46-41f6-98ff-e2c097b4a6f3" 
                 AdUnitId="133638" 
                 HorizontalAlignment="Left" 
                 Height="80" 
                 Width="480"
                 Grid.Row="0"
                 VerticalAlignment="Top" />

            <!--TitlePanel contains the name of the application and page title-->
            <StackPanel x:Name="TitlePanel" Grid.Row="1" Margin="12,17,0,28">
                <TextBlock Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}" 
                           Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
            </StackPanel>

            <!--ContentPanel - place additional content here-->

            <Grid x:Name="ContentPanel" Grid.Row="2">
                <TextBox FontSize="50" Margin="10,11,10,10" Text="{Binding ClockValueString}" 
                     VerticalAlignment="Top"/>
            </Grid>

            <Grid x:Name="StopwatchPanel" Grid.Row="3" >
                <Button x:Name="Start" Width="140" Height="75" Content="{Binding Mode}" HorizontalAlignment="Left" 
                    Margin="10,10,0,0" VerticalAlignment="Top" Click="Start_Click"/>

                <Button x:Name="Reset" Width="140" Height="75" 
                        Content="{Binding Path=LocalizedResources.ResetText, Source={StaticResource LocalizedStrings}}" 
                        HorizontalAlignment="Left" 
                    Margin="140,10,0,0" VerticalAlignment="Top" Click="Reset_Click"/>

                <Button x:Name="Lap" Width="140" Height="75" 
                        Content="{Binding Path=LocalizedResources.LapText, Source={StaticResource LocalizedStrings}}" 
                        HorizontalAlignment="Left" 
                    Margin="270,10,0,0" VerticalAlignment="Top" Click="Lap_Click"/>
            </Grid>

            <Border Grid.Row="4" BorderBrush="Gray" BorderThickness="2" >
                <Grid x:Name="LapDisplayGrid" Grid.Row="4">
                    <ListBox x:Name="lstLapDisplay" FontSize="30" Margin="30,10,20,0"  
                  IsEnabled="True" 
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ItemsSource="{Binding LapCollection}"   
                  Height="320" 
                  VerticalAlignment="Top"/>
                </Grid>
            </Border>





            <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
            <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0,-32,0,0" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" />-->
        </Grid>
    </Viewbox>
</phone:PhoneApplicationPage>